---
name: code-write-read
description: コードを「書く(write)」ときも「読む(read)」ときも常時適用する、『リーダブルコード』と『良いコード/悪いコードで学ぶ設計入門』に基づく可読性・設計ガイド。コードの生成・実装・修正・リファクタリング・レビュー・読解・説明・PR/コミット前チェックなど、コードが関わる作業では言語を問わず(TypeScript, JavaScript, Go, Python, Java, Rust, C++, Ruby, Kotlin, C# など)必ずこのスキルを参照すること。ユーザーが「レビュー」と明示しなくても、コードを書く場面・貼られたコードを読む場面はすべて対象。ただし数行の軽微な修正では出力の重さをスキル内の基準で落とす。読むときは指摘を XXX/FIXME/HACK/TODO の重要度で分類して出力する。
---

# Code Write/Read

コードを書くときも読むときも、同じ物差しを使うためのスキル。

## 根本の考え方

良いコードとは「他の人が最短時間で理解でき、安全に変更できるコード」。
コードの短さより、理解にかかる時間の短さを優先する。

物差しは2軸ある。

- **可読性の軸**(リーダブルコード): 名前、コメント、制御フロー、式、変数のスコープ
- **設計の軸**(良いコード/悪いコード): 凝集度、結合度、不変性、責務の分離

書くときはこの物差しで作り、読むときはこの物差しで測る。

## モード判定

| 状況 | モード |
|---|---|
| コードを新規に生成・実装する | Write |
| 貼られたコード・既存ファイルをレビュー、チェックする | Read(フルレポート) |
| コードの動作説明を求められた | Read(説明主体、XXX/FIXME のみ付記) |
| 既存コードを修正・機能追加する | Read で把握 → Write で修正 |
| リファクタリングを頼まれた | Read で指摘 → Write で修正 |

## 適用の程度

発動すること自体と、出力の重さは別に決める。モードが決まったら、次に規模を見る。

| 規模の目安 | 例 | 振る舞い |
|---|---|---|
| **軽微** | typo修正、変数名1つの変更、ログ1行の追加、既存パターンをそのままコピーした数行の変更 | 原則を黙って当てはめてコードだけ出す。フルレポートは出さない。ただし XXX 相当(握り潰し・null未チェック等)に気づいたら一言だけ添える |
| **通常** | 数十行規模の関数追加・修正、小さなモジュール | Write は原則どおり。Read は依頼された範囲に閉じて、無理に全チェック項目を回さない。指摘があれば簡潔なフルレポート形式で出す |
| **大きい/明示的な依頼** | 新規実装、既存ファイル全体のレビュー、リファクタリング依頼、「チェックして」「レビューして」と明言された場合 | フルレポート。「読む手順」を最初から最後まで通す |

迷ったら軽い方に倒す。過剰なチェックリストは、原則が守ろうとしている「理解のしやすさ」自体を損なう。

---

## Write モード

原則はコードそのものに反映させる。原則の解説はしない。
コードを出す前に、以下を自分でチェックする。

### 命名
- 動詞を具体的に。`get` で済ませず、取得元や処理に合わせて `fetch`(ネットワーク)/`load`(ストレージ)/`find`(検索)/`compute`(計算)などを選ぶ
- 単位や状態を名前に入れる: `timeoutMs`, `sizeBytes`, `unsafeHtml` / `sanitizedHtml`
- 境界を誤解させない: 上限下限は `max`/`min`、両端を含む範囲は `first`/`last`、半開区間は `begin`/`end`
- 真偽値は `is`/`has`/`can`/`should` で始め、肯定形にする(`disableX` より `enableX`)
- `tmp`, `data`, `result`, `info`, `Util`, `Common`, `Manager` のような汎用名を避ける。`tmp` は値の入れ替えなど寿命の短い一時保管だけに使う
- 名前の長さはスコープに比例させる。数行のスコープなら短くてよく、広いスコープほど具体的にする
- 「何であるか」ではなく「何のためにあるか」で名付ける

### 制御フロー
- ガード節で異常系を先に返し、ネストを浅く保つ(目安は2段まで)
- 比較は「変化する値を左、定数を右」に置く
- if/else は肯定条件、単純なケース、重要なケースを先に書く
- 三項演算子は単純な値の選択にだけ使う
- ループ内の除外条件は `continue` で早めに抜ける

### 式と変数
- 長い条件式は説明変数・要約変数に分ける
- 1回しか使わず、説明にもならない中間変数は作らない
- 再代入しない。`const`/`final`/`val`/`readonly` をデフォルトにする
- 変数は使う直前で宣言し、スコープを最小にする
- 完了フラグのような制御用変数は `break`/`return`/標準APIで置き換える
- グローバル変数と、共有された可変状態を避ける

### 関数とクラス
- 1関数1タスク。本題と関係ない下位問題(日付整形、文字列処理、距離計算など)はヘルパーに抽出する
- 関連するデータとロジックは同じ場所に置く(高凝集)
- 生成時に不正な状態を作らせない。コンストラクタやファクトリでガードする(完全コンストラクタ)
- 意味のあるドメイン値は専用の型にする(金額、ID、メールアドレスなど)。ただし規模に見合う場合に限る
- 「尋ねるな、命じろ」: 他オブジェクトの状態を取り出して外で判断せず、そのオブジェクトのメソッドに判断を任せる
- 状態を変える処理と値を返す処理を分ける(コマンド・クエリ分離)
- 引数は少なく。フラグ引数、出力引数、null 引数を避ける
- 継承よりコンポジション。公開範囲は最小限にする
- 同じ条件分岐(種別による switch など)が複数箇所に散るなら、interface による多態へ集約を検討する
- コレクションはクラス内に閉じ込め、外に渡すときは読み取り専用にする

### エラーと安全性
- 例外やエラーを握り潰さない
- null を返さない・渡さない方向で設計する(Optional、空コレクション、専用の型など)
- マジックナンバーは名前付き定数にする
- 文字列に意味を詰め込まない。列挙型や専用の型を使う

### コメント
- コードを読めば分かること(what)は書かない
- 書くのは why: 意図、前提、制約、他案を却下した理由、定数の根拠、落とし穴
- 長い処理やブロックの先頭には要約を書く
- 「それ」「これ」など指示語で曖昧にしない
- 読みにくいロジックをコメントで補うより、コード自体を読みやすく直す
- 既知の課題は `TODO:` / `FIXME:` / `HACK:` / `XXX:` で残す

### やりすぎない
原則の目的は「理解と変更を楽にすること」。原則を守ること自体が目的になってはいけない。

- YAGNI: 「将来必要かも」で抽象化や拡張ポイントを作らない
- 小さなスクリプトや使い捨てコードに、値オブジェクトや strategy パターンを持ち込まない
- 1回しか使わない小さな関数を量産しない。読み手があちこちへジャンプする羽目になる
- 既存コードベースの規約と言語のイディオムを優先する(`references/language-notes.md`)
- ユーザーが明示したスタイル指定が最優先

### Write モードの出力
- コードをそのまま出す。どの原則を適用したかの説明は不要
- 意図的に原則から外した箇所(性能、既存規約との整合など)があるときだけ、1行で理由を添える
- 既存コードを修正した際、依頼範囲外で気づいた問題は直さずに、末尾で Read モードの形式を使って短く列挙する(機能追加とリファクタリングを混ぜないため)

---

## Read モード

### 読む手順
1. **目的をつかむ**: このコードが何をしているかを一文で言う。一文で言えないなら、それ自体が指摘対象
2. **構造を見る**(設計軸): 責務の分かれ方、凝集度、結合度、状態の持ち方、エラーの扱い
3. **詳細を見る**(可読性軸): 命名、制御フロー、式、変数、コメント
4. **重要度で分類して出力する**

### 重要度の定義

| タグ | 意味 | 典型例 |
|---|---|---|
| **XXX** | 危険。障害・データ破損・セキュリティ問題に直結しうる | 例外の握り潰し、null の未チェック、未初期化状態で使えるオブジェクト、共有可変状態の競合、名前と実際の挙動が矛盾 |
| **FIXME** | 誤り、または誤解を招いて次のバグを誘発しやすい | 単位の食い違い、境界が曖昧な名前(`limit`, `stop`)、再代入で値が追えない、コメントとコードの不一致、デッドコード |
| **HACK** | 動くが設計が悪く、変更に弱い | マジックナンバー、フラグ引数、長大な関数・神クラス、密結合、同じ分岐の重複、コピペ、Util/Common への押し込み、継承の乱用 |
| **TODO** | 可読性の改善余地 | 曖昧な命名、深いネスト、説明変数の不足、what コメント、不要な変数、広すぎるスコープ |

迷ったら「放置したら何が起きるか」で決める。

- 障害やデータ事故が起きうる → XXX
- 次にこのコードを触る人が間違った変更をしやすい → FIXME
- 変更のたびにコストが膨らむ → HACK
- 読むのに余計な時間がかかるだけ → TODO

### 出力フォーマット(フルレポート)

既定はプレーンテキスト。絵文字は付けない。分類の見出しだけこのテンプレートに従う。該当なしのセクションは省略する。

```
## Write/Read チェック: <対象(ファイル名・関数名など)>
概要: このコードが何をしているか一文で

### XXX (件数)
- **<場所>** <何が問題か>。<なぜ問題か(読み手・変更者・利用者への影響)>
  → <改善案> 〔<原則名>〕

### FIXME (件数)
### HACK (件数)
### TODO (件数)

### 良い点
- <あれば1〜2点>

### 最初に直すべきもの
1. <優先度の高い順に1〜3件>
```

チームや会話で別の形式(GitHub PR コメント形式、絵文字あり、severity: high/medium/low など)が使われている、または指定された場合はそれに合わせる。分類の考え方(XXX/FIXME/HACK/TODO の判断基準)自体は変えず、表記だけ合わせる。

### 指摘の書き方
- 場所は行番号か関数名・変数名で示す
- 各指摘に「何が」「なぜ」「どう直すか」を入れる。「なぜ」は、誰がどう困るかで書く
- 〔〕内の原則名は短く: 〔早期return〕〔単位を名前に〕〔尋ねるな命じろ〕〔完全コンストラクタ〕など
- XXX と FIXME には、可能な限り短い before/after のコード片を添える
- 同種の指摘はまとめる(例: 「単位のない時間変数 4箇所: L3, L8, L21, L40」)
- TODO が多いときは影響の大きい順に上位だけ書き、残りは件数を示す
- 好みの問題は指摘しない。言語やプロジェクトの規約に反する指摘もしない
- インデントや空白など、formatter/linter が直すものは原則指摘しない

### 説明モード(「このコード何してる?」など)
- 説明を主役にする
- 末尾に XXX と FIXME がある場合だけ、上のフォーマットで短く付記する
- HACK/TODO は件数だけ示し、希望があれば詳しく出す

### 大きなコードを読むとき
- 全体構造(モジュール分割、依存の向き)を先に見て、設計軸の問題を優先する
- 変更頻度が高そうな箇所、ビジネスロジックの中心部に重点を置く
- 行単位の細かい TODO は代表例に絞る

---

## 原則名の語彙集(参照ファイルなしで使える)

〔〕に入れる原則名はここにある分だけで大半をカバーする。参照ファイルを開かなくても、この一覧の意味で使ってよい。

| タグ | 一言の意味 |
|---|---|
| 〔早期return〕 | ガード節で異常系を先に返し、ネストを浅くする |
| 〔単位を名前に〕 | `timeoutMs` のように単位・状態を変数名に含める |
| 〔汎用名を避ける〕 | `tmp`/`data`/`Util`/`Common` など意味の薄い名前を具体化する |
| 〔境界を明確に〕 | `max`/`min`、`first`/`last` など曖昧な限界値・範囲の命名を直す |
| 〔尋ねるな命じろ〕 | 他オブジェクトの状態を取り出して外で判断せず、そのオブジェクトに判断を任せる |
| 〔完全コンストラクタ〕 | 生成時点で不正な状態を作れないようにガードする |
| 〔値オブジェクト〕 | 意味のある値を専用の型にして、取り違えとロジックの散在を防ぐ |
| 〔不変〕 | 再代入・引数の書き換えをせず、新しい値/インスタンスを作る |
| 〔コマンドクエリ分離〕 | 状態を変えるメソッドと値を返すメソッドを分ける |
| 〔低凝集〕 | 関連するデータとロジックが分散している |
| 〔密結合〕 | 他クラスへの依存が多く、変更が波及しやすい |
| 〔下位問題の抽出〕 | 本題と無関係な処理(日付整形など)をヘルパーへ切り出す |
| 〔説明変数/要約変数〕 | 長い式や条件に名前を付けて分割する |
| 〔一度に1つのこと〕 | 1関数・1処理に複数のタスクを詰め込まない |
| 〔YAGNI〕 | 今使わない拡張性・抽象化を先回りして作らない |

これで表現できない、または根拠を詳しく示したい指摘のときだけ、参照ファイルを開く。

## 参照ファイルを読むタイミング

- **フルレポートを書く前**(設計軸の指摘が複数出そうなとき): `references/design.md` で凝集度・結合度・条件分岐整理の具体例を確認する
- **可読性の指摘の書き方に迷うとき**: `references/readable-code.md` の before/after 例に合わせる
- **TS/JS, Go, Python, Java, Rust, C++, Ruby, Kotlin, C# のイディオムと原則が衝突しそうなとき**(例: Go のエラーの返し方、Rust の所有権、C++ の RAII): `references/language-notes.md` を先に読んでから判断する。素の原則をそのまま押し付けない
- 軽微な修正(適用の程度: 軽微)のときは、原則として参照ファイルを開かずに語彙集だけで進める

- `references/readable-code.md`: 可読性の軸の詳細(命名、コメント、制御フロー、式、変数、コードの再編成)と before/after 例
- `references/design.md`: 設計の軸の詳細(凝集・結合、完全コンストラクタ、値オブジェクト、不変、条件分岐の整理、コレクション、メソッド設計、リファクタリングの進め方)と before/after 例
- `references/language-notes.md`: TypeScript/JavaScript、Go、Python、Java、Rust、C++、Ruby、Kotlin、C# での原則の当てはめ方とイディオムとの折り合い
